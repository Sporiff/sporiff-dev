---
import type { Video } from "./types";

const { link, type } = Astro.props as Video;
---

<video controls>
   <source src={link} type={type} />
   Your browser does not support the video tag.
</video>
