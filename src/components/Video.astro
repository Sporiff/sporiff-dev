---
export interface Props {
   link: string;
   type: string
}

const { link, type } = Astro.props as Props;
---

<video controls>
   <source src={link} type={type} />
   Your browser does not support the video tag.
</video>
